package com.gollner.processflow.dto.clients.request;

public record ClientRequestDTO(String name,
                               String document,
                               String email,
                               String phone) {
}
