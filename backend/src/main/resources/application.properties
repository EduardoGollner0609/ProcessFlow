spring.application.name=processflow

spring.profiles.active=${APP_PROFILE:test}
spring.jpa.open-in-view=false
front.url=${FRONT_URL:http://localhost:5173}
api.security.token.secret=${API_SECRET_KEY:secret-key}