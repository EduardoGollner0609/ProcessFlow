export type ClientMinDTO = {
    id: string,
    name: string,
    document: string,
    email: string,
    phone: string
}

export type ClientRequestDTO = {
    name: string,
    document: string,
    email: string,
    phone: string
}